# 在 Netlify 控制台里「Base directory」须留空；后端地址用环境变量 VITE_API_BASE_URL，勿写进 base。
[build]
command = "npm run build"
publish = "dist"

[build.environment]
NODE_VERSION = "22"

[[redirects]]
from = "/*"
to = "/index.html"
status = 200
